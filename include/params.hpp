#ifndef CTP_SWAP_HOLO_AGENT_PARAMS_HPP
#define CTP_SWAP_HOLO_AGENT_PARAMS_HPP

#include <vector>

struct HolonomicAgentParams {

	double size;
	double r_vis;
	double vel_max;
};

struct BaseAlgParams {
	// Empty
};

#endif // CTP_SWAP_HOLO_AGENT_PARAMS_HPP